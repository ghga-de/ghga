# Charts with no workspace member behind them (external or ops-only images).
# Their values stay in deploy/src/values/<name>.yaml and nothing is derived;
# everything else is generated from workspace metadata (see create_charts.py).
charts:
  datahub-monitor:
    description: datahub-monitor
    appVersion: "0.2.3"
    version: "0.2.5"
  remotebackup:
    description: remotebackup
    appVersion: "3.2.7"
    version: "3.3.1"
  test-oidc-provider:
    description: A dummy OpenID Connect provider (OP) that can be used for testing purposes.
    appVersion: "2.2.0-dhi"
