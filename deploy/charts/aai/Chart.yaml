name: aai
description: Local AAI for the self-contained demo/test bed — a generic OIDC provider
  standing in for LS Login (ADR-0007). Default profile is Navikt mock-oauth2-server;
  swap via values for keycloak or point services at an external issuer.
type: application
version: 0.1.0
appVersion: "2.1.10"
apiVersion: v2
