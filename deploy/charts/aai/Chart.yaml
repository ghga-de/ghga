name: aai
description: Local AAI for the self-contained demo/test bed — a generic OIDC provider
  standing in for LS Login (ADR-0029). It runs Navikt mock-oauth2-server;
  production points services at an external issuer instead.
type: application
version: 0.1.0
appVersion: "2.1.10"
apiVersion: v2
