apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
    version: "1"
  name: {{ .Release.Name }}-svc
spec:
  type: ClusterIP
  ports:
    - name: tcp-web
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.parameters.port }}
  selector:
    app: {{ .Release.Name }}
