apiVersion: v1
kind: Service
metadata:
  labels:
    app: user-management
    version: 1
  name: user-management
spec:
  type: ClusterIP
  ports:
  - name: tcp-web
    port: 8080
    targetPort: 8080
  selector:
    app: user-management