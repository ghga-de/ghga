apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
data:
  parameters: |
    {{- if .Values.parameters.default }}
    default:
    {{- toYaml .Values.parameters.default | nindent 6 }}
    {{- else }}
    default:
    {{- toYaml .Values.parameters | nindent 6 }}
    {{- end }}
