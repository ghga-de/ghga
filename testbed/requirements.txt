# What the suite needs on top of the workspace. ghga-connector, ghga-datasteward-kit
# and everything under them are NOT listed here: `just testbed-install` installs them
# editable from uv.lock and resolves this file together with that export, so their
# versions always come from the workspace, and a pin here that disagrees fails loudly.

# Workspace libraries the suite imports itself: name only, for the extras the tools do
# not request; the version is the one in uv.lock
ghga-event-schemas
ghga-service-commons[auth,crypt]
hexkit[all]
openpyxl >= 3.1,<4

# pytest-bdd currently does not work properly with pytest >= 8
# therefore we need to pin pytest to an older version until this is fixed
# see https://github.com/pytest-dev/pytest-bdd/issues/689
pytest>=9.0.3,<10
pytest-asyncio>=1,<2
pytest-bdd>=8.1,<9
pytest-cov>=7,<8
pyotp>=2.9,<3

typer>=0.26
httpx>=0.28,<0.29

hvac>=2.2,<3

#playwright
playwright>=1.6,<2
pytest-html>=4.2,<5
pytest-xdist>=3.8,<4
