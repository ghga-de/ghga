# Integration gate — install the self-reliant ghga-demo umbrella on kind and run the test bed.
# This is what keeps HEAD always integrated (ADR-0009).
# SCAFFOLD: the steps are still TODO; needs deploy/charts/ghga-demo + testbed wiring.
#
# DORMANT: manual-run only. To enable automatic runs, restore:
#   on:
#     pull_request:
#     push:
#       branches: [main]
name: integration

on:
  workflow_dispatch:

jobs:
  testbed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
      - uses: helm/kind-action@v1            # spin up kind (fast, image-loadable)
      # 1) Build only AFFECTED images; pull last-released tags for the rest, then `kind load`.
      #    TODO: drive from scripts/affected_targets.py + [tool.ghga].image members.
      # 2) Install the SAME self-reliant umbrella users get (test-bed profile adds SMS):
      #      helm install ghga ./deploy/charts/ghga-demo -f deploy/charts/ghga-demo/values-testbed.yaml
      #    Envoy Gateway brings Gateway API CRDs; gateway Service is NodePort on kind (ADR-0012).
      # 3) Run the BDD + Playwright suite against the cluster:
      #      uv run pytest testbed/
      - run: |
          echo "TODO: implement once deploy/charts/ghga-demo + testbed/ are in place"
